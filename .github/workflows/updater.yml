name: Update Disposable Email List

on:
  schedule:
    - cron: "0 4 * * *"  # every day at 04:00 UTC
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Run collector script
        run: python src/collector.py

      - name: Commit and push changes
        run: |
          git config user.name "ghostmail-bot"
          git config user.email "bot@ghostmail.io"
          git add data/disposable_emails.txt
          git commit -m "Auto-update disposable email list" || echo "No changes"
          git push
